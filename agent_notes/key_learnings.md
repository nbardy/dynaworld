# Key Learnings

Dense memory bank for surprising lessons learned by trying things. Keep this
file under 200 lines by recompressing it. Raw chronology belongs in
`agent_notes/loose_notes/`.

- `uv runpython` is just a typo, but `uv run <script.py>` can fail differently from `uv run python <script.py>` when the active directory/project is wrong. The durable fix was making `dynaworld` its own `uv` project and running from that project root.
- `uv` already has a shared global package cache by default. A cache env var is only needed to override the default location, not to make sharing happen at all.
- MPS made the tiny TokenGS experiments usable but not automatically fast. Kernel launch/resource contention from another tab can dominate perceived trainer speed.
- Lowering token count is a speed knob, but it also changes representational capacity. Decoupling encoder input size from render/loss size was the better smoke-test knob because it preserved the model input contract while cutting raster cost.
- The naive tiled renderer with nested Python loops was slower than dense PyTorch for the small Mac test case. Tile-based rasterization only pays off when culling/sorting is implemented cheaply, not when Python loops become the scheduler.
- A one-big-tile path being faster than expected showed that "tiled vs dense" was not the real binary. The overhead and code path mattered as much as the raster math.
- For the current renderer, DUSt3R lens metadata beyond pinhole intrinsics/extrinsics is noise. F-stop, lens type, shutter, and sensor metadata do not help unless we add distortion or photometric modeling.
- DUSt3R appeared to run "two passes," but the second phase was global alignment, not duplicate inference. Future logs should label pair inference vs alignment more clearly.
- Stale extracted frame folders are dangerous UX even when the code loads explicit JSON records. Users look at folders, so extraction should clear or isolate outputs to avoid misleading evidence.
- Rendering without camera extrinsics lets the model cheat with frame-conditioned gaussians. For dynamic novel view synthesis, using `camera_to_world` in the renderer is not optional.
- DUSt3R per-frame focal estimates can jitter on a phone clip. Median focal across the sequence is often a better default than trusting each frame.
- A single shared `BaseTrainer` would hide real differences between known-camera, image-implicit, and video-token implicit training. Shared payload contracts are cleaner than shared trainer inheritance.
- Positional tuple outputs were fine for toy iteration but became risky as soon as camera state, videos, and multiple baselines appeared. Named runtime payloads are the right boundary.
- `SequenceData.__getitem__` was useful as a temporary migration crutch, but it hid weak contracts. After trainer call sites moved to attributes and `ClipBatch`, deleting the bridge made violations easy to grep.
- System `python3` can pass compile checks but fail import smokes because dependencies live in the `uv` environment. Use `uv run python` for meaningful Dynaworld checks.
- The smoke wrapper lacked execute permission even though the full wrappers were executable. Always check script mode before assuming a shell script can be run with `./...`.
- JSONC configs are better than environment-variable fanout for trainer experiments. Shell wrappers should choose config files rather than re-declaring every knob.
- Shared JSONC helpers belong in one tiny utility. Keeping `strip_jsonc_comments(...)`, config loading, and W&B-safe serialization in `src/train/config_utils.py` avoids both parser copy-paste and a mirrored Python default map.
- `render_size` should be treated as a viewport, not a crop. Scale `fx/fy/cx/cy` from encoder input pixels to render pixels at the render boundary, then resize GT for loss; pose and FoV stay unchanged.
- Keep full experiment defaults in JSONC, not in a mirrored Python `DEFAULT_CONFIG`. Python should normalize runtime values, validate required sections, and keep constructor-name mapping in one boundary factory.
- On the MPS prebaked-camera baseline, AMP/flash attention was slower because dense splat render/backward dominated. Batched dense rendering improved frame throughput more than reducing `frames_per_step`, which mainly improved iteration rate.
- Do not synthesize higher-FPS camera-bake videos from an already downsampled low-FPS clip. The duplicate/interpolated temporal signal can distort DUSt3R camera estimates; generate bake variants from the original source frames with the final crop/FPS/scale in one ffmpeg pass.
- DUSt3R pose scale is arbitrary enough to break fixed-volume Gaussian heads. The 128px/4fps bake produced camera z translation up to `2.56` while TokenGS initial z is constrained to `[0.5, 2.5]`, putting late-frame Gaussians behind the camera and causing render NaNs before any optimizer update.
- The committed stable prebaked-camera baseline was 32px/2fps/all-frames, not the new 128px/4fps camera trajectory. Treat 64/128 video-size bakes as new camera-distribution tests, not just higher-resolution versions of the same stable run.
- Camera/scene normalization is a pretraining contract, not just a local init tweak. Gaussian extent, camera radius, path residual scale, FoV, near plane, and Plucker moment scale all need a shared canonical frame or each DUSt3R solve can define a different task.
- Upstream `taichi-splatting==0.13.0` is not installable in the repo's Python 3.11 `uv` env, but the vendored fork now has a Taichi 1.7.4/Metal-compatible Torch autograd path. Treat the local fork as the source of truth for Mac renderer experiments, not the pip package.
- The copied `memory_efficient_sparse` rasterizer is not a good MPS benchmark default despite its name. Its per-splat Python-loop custom autograd path was hundreds of ms at 64px/128 splats and seconds at 64px/512 splats, while custom/vectorized sparse stayed around tens of ms after warmup.
- Benchmark summaries must account for skipped optional backends. Timing means over only `status == ok` silently hid Taichi/gsplat on MPS, making the renderer matrix look smaller than what actually ran.
- The checked-in trainers are still tiny for renderer scaling: the prebaked-camera defaults decode 128 tokens x 4 splats/token = 512 splats. A 64k-splat real-time target needs a separate forward-only throughput benchmark; dense `G*H*W` paths are not meaningful at that scale.
- Porting `taichi-splatting` to Mac is not a simple CUDA-shader swap. CUDA sort/cumsum can be replaced with Torch fallback, but the fast rasterizer also uses Taichi SIMT block/warp intrinsics unsupported by Metal. A Metal path needs separate forward/backward kernel structure; the current local patch is a simple global-memory reference path, not a tuned Metal renderer.
- Taichi/Metal benchmark timing needs an explicit Taichi sync at timing boundaries (`TaichiQueue.run_sync(ti.sync)`), not just `torch.mps.synchronize()`. Without that hook, mixed PyTorch/Taichi measurements are easy to mistrust even when the final numbers happen to stay similar after sync.
- The local Taichi/Metal backward path is not just "finite gradients": against a direct CPU-float64 Torch packed-2D raster reference, 16/32px cases with 4/8/16 splats matched forward and packed/features gradients at ~1e-8 absolute error. This validates the core simple Metal raster/backward math, separate from 3D projection correctness or performance.
- Taichi 1.7.4's public `parallel_sort` works on Taichi fields, not Torch tensors, and `PrefixSumExecutor` explicitly rejects Metal. A native Metal bin/sort path cannot just flip the current Torch fallback to Taichi algorithms; custom ndarray scan/sort kernels work on MPS but odd-even sort is far too slow (`128x128/G=65536` around 1.5s).
- A Metal-native ordered per-tile binning path that preserves input order is correct when splats are already depth-sorted, but the naive tile-major implementation is still slower than the Torch-sort reference (`128x128/G=65536` 68ms vs 31ms). The next real win needs a better algorithm, not merely “move sort to Metal.”
- A Taichi-only compact bucket sorter can do the right high-level GPU-binning shape on Metal and passes forward/backward accuracy, but it still loses to the Torch-sort reference (`1024x1024/G=65536` 125.6ms vs 52.1ms forward). Without a fused threadgroup-memory sort+raster kernel, Taichi spends too much time in separate bin/sort/raster stages.
- Low precision is not a simple cast in the Taichi path: the current tile mapper imports f32 `Gaussian2D` and expects f32 depths. fp16 needs separate mapper/raster kernels, not just `features.to(float16)`.
- MLX `mx.fast.metal_kernel(name=...)` requires a valid Metal/C identifier. The raw rasterizer's first cache-derived names contained `=`, `.`, and other config punctuation, causing Metal compile failure before any math ran.
- MLX `mx.eval(...)` is an in-place evaluator in the tested `mlx==0.31.1` path and returns `None`; evaluate first, then read `.item()` from the original array.
- Raw MLX/Metal and the Taichi accuracy reference sample pixels at `x + 0.5`, while the current Dynaworld dense/Torch sparse training renderers sample integer grid coordinates. Use the packed-2D accuracy benchmark for correctness; throughput comparisons against training renderers can show expected image diffs until sampling conventions are unified.
- Torch custom-op schemas declared as fixed tuple returns, e.g. `-> (Tensor, Tensor)`, must be implemented with fixed C++ tuple return types. Returning `std::vector<torch::Tensor>` registers as `Tensor[]` and aborts at extension import with a schema mismatch.
- Do not pass tightly packed Torch `[G,3]` float tensors to Metal kernels as `device float3*`. Metal `float3` has 16-byte alignment/stride while Torch rows are 12 bytes, so splats after index 0 read corrupted conics/colors; use flat `device float*` plus `index * 3` loads or an explicitly packed type.
- Random-splat sort pressure is not currently dominated by Taichi's OBB tile test: exact conic/tile intersection only reduced keys by ~8-10% at 64px/1024 and 128-1024px/64k. Same-tile custom rectangular bounds were ~1.29x exact keys in a 64px/512 probe, so future speed work should check `K/G` before assuming AccuTile will fix Taichi.
- In the Torch+Metal fastpath, saving sorted tile IDs fixed one real backward tax, but v3 still beat v2 while re-sorting in fast backward. That shifts the next optimization model toward parameter bandwidth and global atomic gradient pressure, especially under tile overlap.
- Porting saved sorted tile IDs into v3 confirmed the sort tax is still worth removing: it made forward ~1-2ms slower at 4K/64K because of writeback, but reduced estimated backward ~12-14% and forward+backward ~7-9% in no-overflow synthetic cases.
- Direct Torch projected splatting is only a small-scene baseline, not a 4K/64K comparator. At 128x128/512 it was already ~22-27x slower than v3 forward and ~73-93x slower forward+backward; 4K/64K would be ~1.1e12 pixel-splat evaluations.
- Passing MPS slice views like `packed[b]` through the single-image Taichi autograd function can trip PyTorch version-counter checks during backward. Native Taichi batch rendering avoids that train-loop hazard by keeping `[B, G, ...]` tensors intact through one autograd call.
- Small low-res/low-splat Mac raster benchmarks are launch/overhead dominated, so v2 can beat v3 there even though v3 wins large-scene backward. For bootstrap training at 64-128px with hundreds of splats, choose the low-overhead renderer first; switch to v3 when tile occupancy, wider splats, or 64k-scale gradients make bandwidth/recompute behavior dominate.
- Taichi batch speedup claims are baseline-sensitive. In a fresh `B=4, 128x128, G=128` run, Taichi absolute time was close to the README order of magnitude, but a looped MPS direct-Torch reference was far faster than the README's direct-Torch baseline, shrinking the speedup factor. Compare absolute ms and reference implementation, not just `x` speedups.
- At `1024x1024/G=65536`, Taichi/Metal remains useful as a compatibility path but its backward is not in the same class as pure Metal: measured forward+backward was about `352/1081 ms` for Taichi vs `41/120 ms` for v2 and `20/40 ms` for v3 on sigma `1-5/3-8`.
- The Mac renderer stack now has three distinct proven roles, not one winner: Taichi for compatibility/native batch, fast-mac v2 for tiny bootstrap overhead, fast-mac v3 for large/backward-heavy training. Future renderer selection should be scenario-based, not repo-brand based.
- "Warm" renderer timing means first-use compile/setup effects are excluded, not that rendered splats or outputs are cached. Reusing the same synthetic splats across warmup/timing is fine for kernel timing as long as command sync is correct and no one claims data-randomization coverage.
- Batch support is asymmetric: Taichi has real `[B,G,...]` native batch, while fast-mac v2/v3 are currently single-image APIs looped over frames. This makes Taichi more train-loop convenient than its raw raster speed suggests, and makes a native fast-mac batch API a high-leverage next step.
- fast-mac v5 validates that native `[B,G,...]` Torch+Metal batch support is real and improves forward per-frame throughput (~2x at 4K/B=4 synthetic), but it did not replace v3 for B=1 forward+backward; batching/eval split is an API and forward-throughput win, not automatically a backward win.
- fast-mac v6 showed an active-tile trap: if almost every tile is active, compacting/sorting active tiles and pre-filling the full `[B,H,W,3]` background can make forward ~3x slower. Exposing the existing direct tile kernels and making active scheduling opt-in restored v5-like forward while keeping faster B=4 backward.
- Raster benchmark case labels can lie if conic conventions differ. v5's standalone benchmark initially used `1/sigma` while v2/v3 used inverse variance `1/sigma^2`, changing splat footprint under the same `sigma 3-8` name.
- The raw MLX/Metal path was valuable as a proof of Metal raster viability, but bridge overhead and dependency split make direct Torch custom ops the better training path. Keep raw MLX in `shader_experiments` for provenance/experiments, not as the main renderer backend.
- Very low opacity is not a free stability win for Taichi training. In the 128px/8192-splat baseline, `opacity_init=0.02` stayed finite but slowed to ~1.1-1.3 it/s because rays did not saturate early; `opacity_init=0.1` was a better local default while still avoiding the old near-0.5 opacity saturation.
- A white fast-mac render is not necessarily a renderer/background bug. v5 and Taichi matched forward and decoded-gradient parity on the same 8192 splats, but training could still turn all white because near-camera projection produced enormous xyz/scale gradients and then NaN decoded tensors; the renderer binned no valid splats and returned finite white background. Always check decoded tensor finiteness and use a train-space near plane large enough to keep `fx / z` gradients sane.
- fast-mac v6 active scheduling needs a much tighter auto gate than "some empty tiles exist." In noisy local sweeps, ~15-29% active-tile sparse/layered cases were mixed or slower because active scheduling still pays fixed background/scheduling overhead; the clear active-friendly case was clustered overflow at ~5% active tiles. Treat uniform random as `microbench_uniform_random`, not a real-world default selector.
- fast-mac v7 hardware blending is not automatically the win. After fixing depth order, quad bounds, and backward math it matched a tiny dense reference, and at 4K/64K it beat v6 direct on clustered forward-only (`129.8 ms` vs `253.7 ms`), but uniform forward was slower (`149.6 ms` vs `31.3 ms`) and backward was catastrophic (`~20-21 s`). Until CPU/shared-buffer round trips and backward architecture are replaced, v7 is a profiling branch, not the training default.
- Two-stage "stage-1 world tokens → stage-2 AR novel-view generator" designs re-introduce the camera-leakage they were supposed to fix. Stage-1 tokens silently bake camera-specific info from the reconstruction loss, so stage 2 either expensively patches a broken representation or learns to ignore the tokens and runs as a standalone video model. Prefer one-stage splat-token + fixed differentiable rasterizer designs with multi-camera loss per step (GT camera photometric + perturbed camera diffusion-as-loss); view-independence is a property of the loss, not a second stage.
- Renderer speed and renderer train parity are separate claims, and tiny easy reference tests are not enough. fast-mac v5 looked forward-correct but failed the 32px/23-frame/512-splat train parity run (`~0.177` eval loss vs dense/Taichi `~0.066`) because saturated/crowded backward used per-pixel loop bounds around `threadgroup_barrier()`. The same barrier-control bug was present in v6/v6-upgrade. Barrier control must be tile-uniform; after looping over uniform `stop_count/count` and masking per pixel inside the loop, saturated gradients matched Taichi/reference and the same v5 control reached `~0.070` eval loss.
- After the v5 saturated-backward fix, 128px/4fps local training with `128 tokens * 512 splats/token = 65,536` splats is practical on MPS: no NaNs/OOM over 1000 steps, about `4.13 it/s`, W&B `3piy8cww`, full-video `Eval/Loss 0.10858`, `Eval/SSIM 0.49289`, `Eval/PSNR 18.93`. The final sampled train loss can land on a hard window and should not replace full-video eval.
- A 960-cell v6/v6-upgrade matrix weakened the idea that the chief-scientist upgrade should replace local v6 wholesale. Local v6 won 146/240 workload groups vs upgrade 94/240, but `v6_upgrade_direct` became competitive at 64k splats (median `-0.5%` vs `v6_direct`, 42/80 wins), especially forward+backward. Treat v6-upgrade as a source of mechanisms to ablate, not as the default renderer.
- The "working" bar for DynaWorld is structurally bounded, not aspirational: if the video backbone is frozen and the splat head is a cheap adapter, the ceiling of re-rendered novel views is the backbone's own reconstruction quality. This gives a falsifiable eval — same-camera round-trip should match `base_video_model(clip)` output; novel-camera re-render is adapter quality plus generative capacity on top. Do not pitch or evaluate against absolute photorealism; pitch and evaluate against backbone parity.
- Strategic architecture philosophy lives in `research_notes/meta_philosophy/` (north star, problem doc, mistakes log, prompt-guidance notes, LLM driver prompt), `research_notes/framing_the_problem/` (information-theoretic + contract + patched predictive-quotient framings), and `research_notes/training_contract_v1.md` (operational sampler/signature/loss/export contract). Do not mint `framing_4.md` for the predictive-quotient correction; patch framing 3 and use the training contract.
- Larger `frames_per_step` is mechanically viable with fast-mac v5 native batch rendering at 65k splats, but it is not a free quality win. `B=4/16/23/46` all fit; `B=4` was ~7.9 it/s but only ~31.6 frame-losses/s, while larger batches rose modestly to ~37-38 frame-losses/s; an equal-frame-budget `B=23,350-step` run (`yl8usu29`) underperformed the prior `B=8,1000-step` run on full-video eval (`Eval/Loss 0.1128` vs `0.1086`, `SSIM 0.473` vs `0.493`).
- Video-token implicit `recon_backward_strategy=batched` was a naming trap until it actually called `render_gaussian_frames`; after wiring that through fast-mac v5, the 128px/4fps 8192-splat implicit run learned cleanly in 100 steps (`z60o3ngd`, `Eval/L1 0.1034`, `Eval/Loss 0.1552`) but still trailed the 65k known-camera target (`Eval/L1 0.0723`, `Eval/Loss 0.1086`). Treat it as a working baseline, not yet a parity result.
- The video-token implicit single-frame-reuse bug was real and two-layered: the trainer normalized every sampled window to local `0..1` time, and the model injected time only after query-to-video attention. Preserving absolute sequence times plus injecting `time_proj` before query cross-attention made motion measurable (`gypzvibd`, 500 steps: temporal-to-first L1 ratio `0.458` vs `0.130` before), but adjacent motion is still far too smooth (`TemporalAdjacentL1Ratio 0.037`), so do not treat the implicit baseline as temporally solved.
- The bad 128px video-token implicit render was not a renderer regression: its Gaussian head still zero-initialized all 8192 splats to identical gray opacity-0.5 blobs at `xyz=0`, unlike the bounded-random 8192 known-camera init. Porting `GaussianParameterHeads` fixed the init contract, but 200-step probes stayed around `Eval/Loss ~0.149-0.150`; widening camera range improved accumulated temporal motion but not reconstruction. The remaining gap is baseline-contract/architecture, not just init.
- Image-implicit camera is not generically broken: a paired 32px/2fps/23-frame/512-splat 100-step rerun on the same sequence gave image-implicit `Eval/Loss 0.0603`, `SSIM 0.591` (`mv4ggjq8`) vs known-camera feed-in `Eval/Loss 0.0659`, `SSIM 0.513` (`w3a14cnp`). The 128px degeneration is specific to the video-token/full-scene contract, not the idea of learning cameras by photometric overfit.
- Wiring explicit normalized frame-index time into the video-token encoder, query decoder, and final head tokens did not solve the 128px implicit gap (`febn4gq6`, 200 steps: `Eval/Loss 0.1541`, `SSIM 0.272`, temporal adjacent ratio `0.017`). Time must be explicit for correctness, but the remaining failure is deeper than missing `t`.
- Raising video-token implicit capacity from 8192 to 65536 splats changed behavior more than quality: the 100-step 64k run (`2vllet1i`) stayed stable and unlocked much larger camera motion (`rot 7.19 deg`, `trans 0.604` vs `~1.02 deg`, `0.137` at 8192), but reconstruction improved only slightly (`SSIM 0.284` vs `0.272`). Capacity matters, but the remaining bottleneck is not just "too few splats."
- Fisheye ray generation can be forward-finite while autograd is broken at the principal point: `torch.where(r > eps, x / r, 0)` still evaluates the hidden `0/0` branch and can NaN gradients for focal/intrinsics. Clamp the radius before division/sqrt in differentiable camera math.
- A pure per-splat Metal VJP for pinhole `3D->2D` projection is the right v8 move. Replacing Torch-rematerialized projection backward cut warm MPS forward+backward from v5 `21.8ms` to v8 `11.1ms` at `128px/8192`, and from `41.2ms` to `22.3ms` at `256px/65536`; forward also improved at large count (`11.8ms` -> `5.8ms`). The main grad diff is camera-pose atomic accumulation (`~3e-2` abs on `~2e4` grad), while per-splat means/scales/quats stay near fp32 noise.
- The named v9 project3d-train fork preserves the same lesson with a clean namespace and benchmark path: staged fusion beats a literal mega-kernel. On warm MPS `--warmup 5 --iters 10`, v9 reduced `256px/65536` from v5 `16.63/39.26ms` forward/forward+backward to `5.92/17.32ms`, while keeping image max/mean error at `2.2e-5/2.2e-8`.
- Full `v5/v8/v9` project3d sweeps need all-case gradient checks and more than one timing row. With `--grad-check all`, v8/v9 matched v5 images at `~1e-7..1e-5` max error and per-splat gradients near fp32 noise, but MPS train means were noisy enough that v9 was not uniformly faster than v8 despite the cleaner namespace. Keep v8/v9 side by side until a median/p95 harness or fused next kernel settles the winner.
- PowerFoam power-face signs are a real footgun: the correct plane uses `+ r_i^2 - r_j^2` for cell `i` clipped by neighbor `j`. The first local Metal prototype and its CPU mirror both had the radius terms flipped, so mirrored reference tests alone would not catch the paper-equation error.
- Do not conflate direct GS fit init with token-GS init. `FreeDynamic3DGS` fit samples first-frame pixels for xyz/color, while the strong token-GS configs can intentionally use uniform RGB init; a random step-1 token video does not prove the direct GS fit baseline starts random. PowerFoam smoke should use image-seeded init when the comparison target is direct GS fit.
- Image-grid color seeding is not enough to make the toy PowerFoam renderer line up. Step-0 W&B logs still showed poor init (`16f/32 cells L1 ~0.206`, `1f/64 cells L1 ~0.256`), so the missing pieces are likely the official surface/texel/color-query model and adjacency, not just random RGB init.
- Full PowerFoam-style texel/plane/view-color attributes fix the local "blurry solid cells" issue at single-image scale: 128px/128-cell reference went from step-0 `L1 0.0720` to step-100 `L1 0.0369`, slightly ahead of the earlier direct GS single-image `L1 0.0382`. The video path needs param groups/Metal speed; single large LR destabilized 16-frame eval.
- PowerFoam camera-facing init has to rotate the official +x normal to `-z`, not `+z`. The wrong quaternion sign made the new normal loss penalize step 0 (`normal_loss ~0.82`); after flipping the sign, the 64px full tiny smoke initialized at `L1 0.0483` and step 2 reached `L1 0.0393` with `normal_loss 0`.
- The first trainable PowerFoam Metal path is a core bounded-cell renderer, not full paper PowerFoam. Its streaming replay backward matches Torch autograd to `~3e-6` on points/radii and trains 256 per-frame cells at 64px/16f from `L1 0.0547 -> 0.0362` in ~1.2s train-loop time, but visuals remain cell-like because oriented planes/detail-site/SV texture/densification are still absent.
- Adding Metal midpoint-linear cell texture is a cheap quality step but not the full fix: 1024 cells/64px/16f improved eval from constant `L1 0.03016` to linear `0.02786`, with only ~5-6% kernel total-median overhead at 128px/1024, yet previews remain cell-based. The next quality jump still needs the real oriented surface/detail-site/SV model.
- Fixed surface-linear Metal sampling is a quality win only after opacity calibration: `density_init=16` collapsed to stippled coverage (`L1 0.0790`), while `density_init=64` reached `L1 0.02378` at 1024 cells/64px/16f and beat midpoint-linear. The cost is real too: 128px/1024 fwd+bwd rose to ~23.96ms vs ~11.69ms for midpoint-linear, so learned surfaces need speed work before becoming the default.
- Learned Metal surface normals are correct and trainable but not the main missing piece on the tiny front-facing video: oriented-surface reached `L1 0.02367` vs fixed surface `0.02378`, with normal delta `~0.12` and similar `~22.5ms` fwd+bwd cost at 128px/1024. Expect the next meaningful quality jump from detail sites / SV color / densification, not normals alone.
- Learned local detail sites are the first meaningful Metal PowerFoam quality jump after surfaces: oriented texel-surface reached `L1 0.01899` on the 64px/16f/1024-cell smoke, with center/normal/texel-site drift all nonzero. This validates the replay-backward contract for per-site texture without `N*H*W` grad storage, but it is still not official PowerFoam until SV view color, heights, regularizers, and densification are added.
- For depth-of-field modeling, focal length alone is not enough. The identifiable renderer quantity is effective CoC strength `Q = F * D = F^2 / f_number` plus focus distance; without EXIF/sensor scale/metric scene calibration, f-stop is a UI/calibration label rather than a monocularly recoverable physical parameter.
- The first 16f V-JEPA A/B was a smoke, not a fair quality verdict: it used a 128px raster/loss target while the HF fpc16/256 encoder internally saw 256px features, ran only 250 steps, and optimized full-frame sky/grass-heavy loss. Treat V-JEPA evidence as unresolved until the local and frozen-encoder runs are 256px end-to-end with step-0 media.
- Step-0 render logging and init diagnostics are now part of the video-token baseline contract. The model can start with weak RGB diversity and low same-split inter-token variance, so missing foreground detail should be debugged by checking initialization media/spread metrics before blaming the renderer or pretrained video backbone.
- The combined RGB-uniform plus stronger query/head init ablation (`d8i7kw6f`) improved the 128px local video-token implicit run (`Eval/Loss 0.1482 -> 0.1444`, `SSIM 0.293 -> 0.315`, temporal-to-first ratio `0.337 -> 0.494`) and visibly recovered more high-frequency dog/detail structure, but still did not solve animation (`TemporalAdjacentL1Ratio 0.0446`). Treat it as an init/detail win, not a complete baseline fix; the original pair is also suggestive rather than clean because fast-mac was defaulting to `inputs_sorted_by_depth=True` for unsorted decoded Gaussians.
- The 16f/128px single-video comparison is not a clean test of video conditioning: unconditioned learned tokens plus the Gaussian decoder matched or beat local/V-JEPA eval (`xenc4w06 Eval/Loss 0.1439`) while direct free per-frame splats failed badly (`kttrbewl Eval/Loss 0.2633`). The decoder/head parameterization is doing real optimization work, and single-clip overfit can be solved from time alone; conditioning claims need 256px source-detail, held-out windows/clips, or scene-distinct eval.
- Visual sharpness and scalar losses diverged on the no-conditioning controls: free splats looked more edge-aware despite bad L1/PSNR, and init probes do not support "token models start with larger splats" as the cause. Local video-token init had similar scale P99 to free splats (`~0.040`) but near-gray RGB diversity (`RGB std 0.011`, entropy 0.232) and low cross-token xyz spread. Debug dog/detail failures with foreground, high-pass, motion-masked, and decoded-state metrics instead of only full-frame reconstruction loss.
- The precomputed-feature trainer bakes the whole loaded `SequenceData`, not the sampled `train_frame_count` window. For Mac-fast V-JEPA 2.1 ViT-B/384 runs, leaving `data.max_frames=0` made it try all 46 frames and spend minutes in MPS attention with ~25GB peak footprint; cap loaded frames or add clip-aware caching before calling a feature path "fast."
- The static/dynamic split plus precomputed V-JEPA 2.1 ViT-B/384 features is now the strongest tiny same-source baseline: 250 steps reached `Eval/Loss 0.088`, `SSIM 0.611`; the visually "almost perfect" run `mybv736f` used the 1000-step config but was interrupted after the step-500 video checkpoint at `Eval/Loss 0.0547`, `SSIM 0.784`, temporal adjacent ratio `0.801`. Credit the recipe plus ~500 steps, not a completed 1000-step schedule. This remains source-view overfit evidence, and camera motion also increased, so validate on scene-distinct/multicam before claiming novel-view generalization.
- The completed matched static/dynamic matrix says V-JEPA still helps, but the reason is narrower than the early story. Unconditioned 96/32 static/dynamic strong-init tokens reached `Eval/Loss 0.0588`, `SSIM 0.771` at 1000 steps (`qstqjup2`), so split/init/decoder optimization explain most of the old win; matched V-JEPA 1000 improved to `Eval/Loss 0.0455`, `SSIM 0.834` (`x4uc6va3`) but also had much larger adjacent camera motion (`0.291 deg`, `0.0355 trans`). Treat V-JEPA as helpful for same-source reconstruction, not proven as true scene-dynamic learning until camera-clamped or held-out-camera controls agree.
- The camera-clamped static/dynamic control strengthened the V-JEPA claim rather than falsifying it. With camera bounds tightened to `FOV +/- 3 deg`, `radius_scale 1.1`, path rotation `1 deg`, and translation ratio `0.03`, V-JEPA 96/32 static/dynamic strong init (`yhezacn8`) beat the matched unconditioned clamp (`mqaz9ohc`) by a wide margin (`Eval/Loss 0.0425` vs `0.0805`, `SSIM 0.848` vs `0.651`, `PSNR 25.26` vs `21.10`). It also slightly beat the unclamped V-JEPA 1000 run while cutting adjacent camera motion by about half and mean rotation from `6.80 deg` to `1.12 deg`, so the previous win was not only camera compensation. This is still same-source overfit, not scene-generalization proof.
- Gauge-field support modes proved why source-view PSNR cannot select a 3D primitive: `oriented_slab` won first source-view support benchmarks, but the first calibrated DeepView held-out lane ranked `free_dynamic_3dgs`/`screen_disk` above it. Keep `screen_disk`, `oriented_slab`, `rank_adaptive_metric`, and `free_dynamic_3dgs` as a falsification suite until held-out cameras, X-map non-collapse, witness rank, cheat probes, and equal active-parameter budgets agree.
- Sintel-era .blend files (Blender Internal renderer, pre-2.80) cannot be re-rendered correctly in modern Blender. Both EEVEE and Cycles auto-substitute Principled BSDF approximations for unsupported BI shader nodes, producing pink-blob characters and broken depsgraph cycles. Use Blender 2.79b for any Sintel/Big-Buck-Bunny/Elephants-Dream-era render; modern productions (Spring, Sprite Fright onward) are Cycles-native and stay in modern Blender.
- Each per-shot .blend file in a Blender Studio production (`02.a.blend`, `02.b.blend`, ...) is the authoritative source for that shot's frame range, active camera, and animation state. Don't open one shot's .blend and switch to another shot's camera object via Python — the camera's matrix_world will be at default/rest position and characters will be in the wrong pose. Open the right `02.X.blend` for shot X.
- Blender Studio's per-shot .blend files use timeline-marker camera binding (`scene.timeline_markers[i].camera`). On `frame_set(f)` the active camera is rewritten from the marker, overriding any earlier `scene.camera = obj` assignment. Clear `m.camera = None` for all timeline markers before rendering with a custom camera.
- Sintel-style .blend files ship with `scene.layers = [True at L01 only]`, with the camera and ~47 main objects on L09. The production team had a render-farm prelude that enabled all 20 layers. Headless `blender -b file.blend -a` would render almost-empty content. Set `scene.layers = [True]*20` before rendering.
- Sintel ships at `resolution_percentage = 25` (production preview); silently divides whatever resolution you set by 4. Force to 100 explicitly.
- Linked-library group instances in Blender 2.79b carry their own Lamp objects that don't appear in `bpy.data.lamps` at file-open time. Iterating `bpy.data.lamps` to scale energy only catches a handful of direct datablocks; the bulk of the production lighting (group-instance lamps) stays at full intensity. To dim properly, traverse `bpy.data.groups` or work via `world.exposure`/exposure compensation instead of per-lamp scaling.
- Sintel-era .blend's `scene.render.image_settings.file_format` enum is runtime-locked to FFMPEG/AVI variants when the scene is in video mode, even though `bl_rna.properties["file_format"].enum_items` reports all 16 formats. Assignment of any non-video format raises `enum "X" not found in ('FFMPEG')`. Workaround: `bpy.ops.render.render(write_still=False); img = bpy.data.images['Render Result'].copy(); img.file_format='PNG'; img.save(); bpy.data.images.remove(img)`. The `.copy()` step matters — `Image.save()` invalidates the original Render Result so subsequent frames render empty.
- Blender 2.79b headless has a multi-camera-per-script bug where setting `scene.camera` in a loop and rendering N times produces N byte-identical PNGs. The fix is bash-looping separate Blender invocations (~30s setup overhead each). Single-camera-per-invocation reliably renders the camera you asked for.
- Blender Internal renderer's raytraced AO across Sintel's 3.3M verts × 4.9M faces × 17 lamps cannot finish "Occlusion preprocessing" on Apple Silicon Rosetta in reasonable time (15+ min observed without progressing to actual rendering). Disable `scene.render.use_raytrace`, `world.light_settings.use_ambient_occlusion`, `use_indirect_light` for tractable renders. Trade-off: severe overexposure since direct lamps dominate without AO/indirect to soften them. SSIM vs released film stays around 0.25 in this regime; pushing higher requires real raytracing or better global lamp scaling.
- The Durian SVN (`download.blender.org/durian/svn/`) is permanently 404 and was never preserved publicly. The only surviving Sintel production source is the archive.org `sintel-dvd` 4-DVD ISO torrent (29.4 GB, perpetual webseed, CC-BY 3.0). The opening fight (`01_snowbandits`) is layout-only on the DVD — its lighting blends were never published anywhere.
- Blender Foundation Open Movies bifurcate by render era: the Spring/Sprite-Fright/Charge/Cosmos-Laundromat tier is on Blender Studio behind a $11.50/mo subscription; older Sintel/Big-Buck-Bunny/Tears-of-Steel/Elephants-Dream tier is fully free via download.blender.org and archive.org. CC-BY licensing applies to production .blend files in both tiers (CC-BY-ND only applies to finished films). Tears of Steel's actors are live-action plates — only its CG VFX can be re-rendered from novel cameras.
- ViVo compact camera MP4s can start at different capture timestamps, so a unified multicam loader must choose per-camera `source_start_seconds`/`target_start_seconds`; using the source offset for all cameras silently desynchronizes target/held-out frames.
- The current fast-mac v5/v5_features MPS path is hard-float32 at both wrapper and bridge boundaries: `fast_mac.py` casts projected inputs to `.float()`, Python bridge checks reject fp16/bf16, and C++/Metal checks assert float32. A true low-precision render test needs a forked variant, not an AMP/config flip. On 128px/16f/8192, projected raster fwd+bwd is only ~18-19ms while full model backward is ~115-205ms, so fork low precision only after a larger regime proves raster dominates.
- The old 15-30 it/s TokenGS/freeGS memory matches single-frame or tiny-frame-count runs, not current 16-frame video training. Measured 128px/8192 single-frame is ~30-33 steps/s, while 16-frame video is ~6.8-8.3 steps/s but ~108-132 frames/s; report frames/s for video throughput.
- Full-step high-resolution single-frame benchmarks couple frame resize, render, pixel loss, and backward, so 4k should not be read as pure raster speed. At 8192 splats, 512px remains local-trainable (~17-21 steps/s), 2k is probe-speed (~2.4-2.7 steps/s), and 4k drops below 0.5 steps/s mostly from sampling/resize plus image loss/backward.
- The current trainer fast-mac path still uses `variants/v5` for RGB `F=3`; newer v6/v8/v9 trainable forks are present but not wired into `src/train/renderers/fast_mac.py`. A 4k/1f/8192 free-splats v5 phase probe showed projected raster forward median ~59.5ms, projected raster backward median ~180.7ms, and noisy mean backward ~483ms, enough to justify a focused v5-vs-v6/v8/v9 high-res backward audit before trainer integration.
- The high-res fast-mac matrix confirms v5 is the wrong RGB path for 4k/64k training: v5 measured `160.7ms` total / `112.3ms` backward, while v6/v6_refined/v9-hw-tile measured `59-62ms` total / `~48-49ms` backward. The remembered `~30ms` backward class exists at 2k/64k on newer forks, not on v5.
- `v6_refined` is a promising TokenGS trainer swap but not a universal default. It sped unconditioned TokenGS by `1.69-1.80x` on 128px/1f, 128px/16f, and 512px/1f, but free-splats 16f regressed (`23.0 vs 29.5 fps` at 128; `15.5 vs 20.4 fps` at 512), so promotion needs a scheduler/graph audit plus quality parity.
- Fixed-seed 20-step quality smokes did not show RGB `v6_refined` accuracy degradation versus v5 on 128px/16f TokenGS or free-splats; eval loss/PSNR/SSIM were marginally better. The default still should not flip until the mixed throughput story is resolved and feature splatting has an equivalent speed path.
- `v6_refined_features` now exists as an isolated feature-splatting namespace and passes F32/alpha/reference plus trainer smoke gates, but it is not yet the v6_refined speed path for F32. The standalone F32 matrix remains roughly parity/slower than `v5_features`, so the real next task is porting v6_refined active/adaptive kernels to arbitrary feature channels.
- The first big F32 feature-splatting speed win belongs in a fork, not the stable baseline: `v6_refined_features_f32_reduce` preserves `v6_refined_features` untouched and replaces per-pixel/per-channel `g_colors` atomics with SIMD/threadgroup reductions. With trainer-like `GSP_CHUNK=64,GSP_FAST_CAP=2048`, local `B=16,G=8192,F=32,512px` raster timing improved from stable `1001.9ms` total / `913.7ms` backward to fork `449.2ms` total / `326.2ms` backward, and frozen colors reached `267.7ms` total / `179.8ms` backward. Trainer promotion still needs phase traces because dense `[B,H,W,F]` tensors remain a likely bottleneck.
- Feature-splatting benchmark caps can completely change the story. The early safe runner forced `GSP_CHUNK=32,GSP_FAST_CAP=512` and made F3 feature paths look ~30x slower than RGB; with the trainer-like `64/2048` cap, 128px/B16/G8192 F3 feature forward matches RGB (`~12ms`) and only stable feature backward remains slower (`28.8ms` vs RGB `15.5ms`). Use `cap=512` only as fallback-pressure stress, not primary throughput evidence.
- F32 feature staging is not automatically a Metal win: staging `[GSP_CHUNK,F]` colors in threadgroup memory regressed local `B=16,G=8192,F=32,512px` train raster total, likely from threadgroup-memory/occupancy pressure. Thread-local F<=32 output accumulation looked eval-only under the old cap and beats reduction on synthetic fwd+bwd after the cap fix (`388.1ms` vs `449.2ms` at 512px), but the 256px fixed trainer graph still favors reduction (`658.9ms` vs accum `665.5ms` median). Re-test mechanisms after cap/fallback corrections, then gate them in trainer context.
- Tiny and modest 128px trainer phase traces are not sufficient promotion gates for F32 raster forks. `f32_reduce` can look better on total step (`292ms vs 363ms` at 64px, `403.5ms vs 439.9ms` on the 128px multicam smoke) while detached raster-backward probes look worse (`20.9ms vs 13.2ms` at 64px, `81.5ms vs 74.0ms` at 128px) because colorize/loss, projection, and model backward remain large. Promote from full phase traces plus heldout-quality parity, not raster-only wins.
- At 256px multicam F32, full-step trainer timing was too noisy to promote `f32_reduce` (`1147.8ms` median vs stable `980.3ms` in one warm2/iters4 run), but the new seeded fixed-render graph mode isolates the renderer/loss path and shows the real shape: `f32_reduce` is a small timing win over stable (`658.9ms` vs `673.3ms` median), and frozen colors are much faster (`608.9ms`). This is timing evidence only; default promotion still needs heldout-quality parity.
- Forward/backward parity can be checked cheaply before spending W&B time: `src/benchmarks/fixed_render_variant_parity.py` showed exact feature/alpha/RGB/loss parity at 256px multicam F32 for stable `v6_refined_features` versus both `f32_reduce` and `f32_accum`, on both train and heldout render targets; the 128px `--check-gradients` gate matched decoded sequence grads within `<=9.61e-10` and colorize grads exactly. This is not trained heldout-quality parity, but it catches trainer-path renderer drift before long runs.
- For camera-only or frozen-feature follow-up, skipping color-gradient atomics is not enough; the C++ op must also avoid allocating the dense `g_colors` grad tensor. In `f32_reduce`, returning an empty `g_colors` tensor and passing a 1-float placeholder kept contracts passing and measured `B=16,G=8192,F=32,512px --freeze-colors` at `362.2ms` total / `242.5ms` backward.
- F64 feature timing is shape-dependent enough to benchmark before picking the fork, but naive true-F64 local accumulation is a negative so far: `v6_refined_features_f64_accum64` raises `GSP_LOCAL_ACCUM_CAP` to `64` and passes F64 gradient/alpha gates, yet loses to existing forks at `256px/B16/G8192/F64` (`682.4ms` vs `646.4ms` best existing) and does not win `512px/B8/F64` (`567.0ms` vs `542.7ms`). A future F64 accumulator needs lower private pressure, such as two 32-channel blocks.
- Full-vector grad-feature caching is noisy but still live as an opt-in fork: `v6_refined_features_f32_gradcache` caches `grad_features[pix, :]` for `F<=32`, wins `256px/B16/F32`, loses `256px/B32/F32`, lost one early `512px/B16/F32` matrix, then won a later `512px/B16/F32` confirm and same-session 256px trainer fixed-render gate. The `block4` reuse attempt regressed; promote neither without heldout-quality parity.
- Fixed-cap binning can beat exact-size bin allocation at the target row but is not free: `v6_refined_features_f32_fixedbin` removes `cumsum/cat/clone/item` and wins `512px/B16/F32` (`501.8ms` vs `706.5` accum / `716.4` gradcache / `855.4` reduce) and slightly wins `256px/B16/F64`, but loses `256px/B16/F32` and only ties accum on trainer fixed-render. Keep it no-overflow opt-in until heldout-quality training validates it.
- Trainer-path sampled memory weakens fixedbin as a memory fix: at 256px fixed-render, `f32_fixedbin` and `f32_gradcache` both run near `649ms`, but fixedbin samples `1.70GB` current allocation while stable/gradcache/accum sample `1.41GB`; the fixed ID buffer is visible inside the real graph even when timing improves.
- Render/loss microbatching is the largest current F32 memory lever, but it changes the speed ranking: benchmark-only fixed-render chunked backward at 256px cuts sampled current allocation from `1.41GB` to `0.57GB` at chunk size 8 and `0.37GB` at chunk size 4. Stable pays about `14%` time at chunk size 8; `f32_gradcache` lost its batched-mode speed edge in the shared-background chunked rerun. Wire this only with trainer parity/quality checks.
- Chunked backward itself is not numerically suspect on the fixed render/loss graph: `fixed_render_backward_mode_parity.py` shows chunk8 vs batched backward matches loss to `7.45e-09`, sequence grads to `<=8.44e-10`, and colorizer grads to `4.05e-08` for both stable v6 features and `f32_gradcache`. The remaining risk is real trainer plumbing around camera-swap relpose/regularizers, not the render/loss accumulation math.
- Whole-step learned-residual training confirms the memory ceiling is above the raster fork: one real 256px `trainer.step()` with all four camera-swap pairs peaks at `2.93GB` current allocation for both stable and `f32_gradcache`, while `f32_gradcache` is faster in that one smoke (`3952ms` vs `4822ms`) with identical loss. Sampling fewer camera-swap pairs is the largest measured real-step lever (`0.90GB` for one pair, `1.60GB` for two), but it changes the per-step objective and needs heldout-quality validation.
- Full camera-swap param-gradient parity on MPS needs a stable-vs-stable control: the learned-residual graph shows order-one `model.video_encoder.input_norms.*` LayerNorm parameter-gradient diffs even without switching kernels. `f32_gradcache` has exact loss parity and no extra non-input-norm drift versus that control (`1.05e-06` vs `1.71e-06`), so use fixed-render sequence-grad parity plus the excluding-input-norm field instead of raw max-param diff.
- Compact-basis feature lookup is algebraically sound and can be faster before it is a general memory win: `v6_feature_lookup_experiment` matches direct full-F rendering and gradients when `features = feature_weights @ lookup`; bounded 128px/B16/G8192/F32 probes show K=4/8/16 lookup faster and lower sampled current allocation than direct. The current ID helper densifies to `[G,K]`, the final `[H,W,F]` tensor still exists, and smaller-G memory was mixed; require trainer fixed-render or hardware-memory evidence before dispatch.
- The first source-anchored two-video relpose path is trainable and beats the old DeepView 3-cam heldout row, but only under a strict leak guard. At 250 steps, V-JEPA compact conditioning plus `W_source` decode and a tiny target-aware SE(3) residual head reached heldout camera_0040 PSNR `14.0453` vs free dynamic 3DGS `13.2940`; target features must stay confined to the relpose head, and heldout eval must use calibrated/query deltas rather than heldout RGB.
- Full height+SV PowerFoam ray-walk replay is now trainable with normal-distance loss on Metal: the raytrace wrapper matches raster on small full-primitive forward/backward fixtures including `normal_distance` (`<=3.36e-08` gradient error) and, after emitting the extra plane, measures `1016.1ms`/`1014.4ms` fwd+bwd at 4k for 1024/4096 cells with a replay event-cap guard. Caveat: this is mode-6/current-adjacency replay with fixed cap 64; depth-quantile/external-normal gradients and paper-scale acceptance are still open.
- Optional regular-triangulation topology is now real and render-verified but not fast on the current synthetic lane: lower-hull lifting via SciPy/Qhull matched unweighted Delaunay exactly, and dense-vs-regular raytrace parity passed on tiny constant/height+SV scenes including backward (`<=1.02e-07` grad error), yet 4k height+SV train totals are still `1868.7ms`/`2888.3ms` after normal-distance refresh because average degree rose to `~13.7` and walk steps increased. Keep Cech/AABB selected for speed until regular graph traversal/replay is optimized or required by a real-scene correctness gate.
- PowerFoam LR warmup must be applied before the optimizer step: the trainer originally updated group LRs after `optimizer.step()`, so density/radii/height warmup groups took one oversized first update. Moving LR update before forward/backward fixed that bug and 32px raytrace smoke still passed; the 128px official-style recipe still degraded, proving warmup was real but not the only schedule issue.
- The 128px/16f PowerFoam Metal raytrace DeepView lane now separates backend scale from optimization quality: 1024 cells / 80 steps runs without event-cap failure, and a low-geometry/no-aux control improved train PSNR `7.7303 -> 8.0554` with controlled quaternion motion (`mean_delta 0.104`), but heldout peaked early at `8.1417` then fell to `8.0881`. A color-only freeze improved train PSNR to `8.0430` while heldout still selected step 0 and fell to `8.0730`; a train-camera-only plane-sweep PLY was worse (`7.7779` best heldout). The gap is not just geometry LR over-motion or any shallow two-view point cloud; current init/contract is weak for NVS.
- Do not raise the PowerFoam raytrace replay cap just to make high-capacity real-scene runs fit: a 4096-cell DeepView probe needed cap128 (`max_steps=75`) and completed, but heldout selected step 0 (`8.1776`) and final fell to `7.8097`; cap128 also slowed the selected synthetic 4k fwd+bwd benchmark to ~`3.1s` from the cap64 ~`1.0s` class. Keep cap64 selected for fast 4k until a second specialized fallback kernel or traversal fix exists.
- EX4DGS/Neural3D point-cloud init is viable only when transformed into the trainer's anchor-camera coordinates and given a scene-scale PowerFoam box, but raw unfiltered sampling is weak. The filtered `coffee_martini` EX4DGS input kept `5113/5498` points at `xy_extent=24`, `z=4..120`; the 1024-cell Metal raytrace run improved heldout `cam06` PSNR `10.0330 -> 10.7416` in 40 steps and the longer 200-step run selected step 150 at `11.9795`. A raw world-space `multicam_world` 1-step smoke without filtering started at only `5.8758`; adding first-class `train_visible` point-cloud filtering in the trainer reproduced the `5113/5498` filtered count and `10.0330` step-0 heldout PSNR from the prepared artifact. This is the first positive real-scene SfM-style init signal for PowerFoam Metal, but it is an external pretrained EX4DGS artifact, not a clean paper benchmark.
- Train-only ORB feature triangulation is too weak to stand in for the paper's SfM/COLMAP init on the current Neural3D coffee split. The train2 artifact from `cam04/cam09` kept only 89 points and heldout stayed at `5.6311` PSNR; a stronger train4 artifact from `cam04/cam09/cam13/cam20` kept 662 points with median reproj `3.29px`, improved train PSNR to `5.9217`, but heldout still selected step 0 at `5.6727` PSNR. This closes a clean negative control: the next paper-clean init needs real COLMAP/pycolmap or a stronger multi-view track builder, not another shallow pair matcher.
- Known-pose pycolmap itself is not enough if tracks are sparse: train4 1024px SIFT verified all 6 pairs and extracted 18,005 keypoints, but triangulated only 44 box-filtered two-view-track points for frame 0. Merging per-frame snapshots for frames 0/4/8/12 raised the clean artifact to 227 points and visible fraction to about `0.153`, but heldout still selected step 0 at `5.6309` PSNR while source PSNR only reached `5.9467`. The next paper-clean improvement needs dense/longer multi-view tracks or a better scene/source, not just swapping ORB for pycolmap SIFT or unioning sparse per-frame clouds.
- DeepView can produce a denser clean pycolmap artifact only when the builder allows per-image intrinsics and more than two train cameras. The 2-camera probe made only 17 raw points and none survived the default `z=1..3.25` box, but an 8-camera 256px known-pose probe verified all 28 pairs and produced 639 points with median reproj `3.32px` (mostly two-view tracks). The 16f Metal run consumed 599 train-visible points and trained source (`7.7475 -> 7.8075`) while heldout stayed step-0-flat at `7.8250`; the matching frame0-only run improved heldout `7.7417 -> 8.0377` with nonzero center/density/feature/quaternion/texel/SV deltas. That points to static/dynamic temporal mismatch or schedule/normalization as part of the clean-init failure, but still does not beat the image-depth low-geometry control (`8.1417`).
- Longer frame0 training is not automatically better for clean PowerFoam. The DeepView 8cam pycolmap frame0 run improved heldout to `8.0377` at 40 steps, but a 200-step extension with the same absolute warmup semantics overfit source (`8.4144` source PSNR) while heldout fell to `7.8695`; a low-motion 200-step variant capped geometry/density drift and recovered only `7.9646` best heldout. Treat the 40-step row as the selected clean-frame0 checkpoint until the schedule is redesigned, not as evidence that more steps will close the paper gap.
- Higher-resolution clean pycolmap can matter, but "more SIFT pixels/points" is not monotonic. The DeepView 8cam 512px SIFT artifact grew from 639 to 771 points and remained mostly two-view tracks (`mean 2.01`, `p90 2`), yet raised clean PowerFoam Metal heldout to `8.4810` on frame0 and `8.5355` on the 16f companion, beating the earlier image-depth low-geometry control (`8.1417`). Longer 512px schedules did not help: 80-step dense eval selected `8.5345` then fell to `8.4564`, and 120-step low-motion/low-appearance reached only `8.5270`. A 1024px SIFT artifact grew to 975 points/948 train-visible cells but worsened reproj (`median 3.82px`, `p90 6.79px`) and heldout selected step 0 at only `8.4415`. Direct long-track probes are worse: loose merge/transitivity at 512px leaves only 19 points with track length >=3, and known-pose guided verification leaves only 1. Opt-in duplicate jitter for underfilled 512px init also drops heldout to `8.3993`. The selected 512px recipe is init/schedule limited, and the paper-clean gap needs a different matcher/source or dense long-track geometry, not just higher extraction resolution or duplicate cleanup.
- Local and Modal-pip pycolmap 4.0.4 expose ALIKED/LightGlue APIs/enums but the installed wheels lack ONNX support; Mac `sift_lightglue` aborts with `LightGlue feature matching requires ONNX support`, and Modal Linux ALIKED aborts with `ALIKED feature extraction requires ONNX support`. Guard ONNX-backed builder modes and run ALIKED/LightGlue geometry only in an ONNX-enabled pycolmap container/host.
- The direct Torch PowerFoam reference must receive full rays for multiview correctness: fixed-origin assumptions affected power-order sort, sphere/face clipping, surface queries, and SV view color. `render_powerfoam_torch(...)` now accepts `[origin,direction]` rays and passes translation-invariance plus SV-origin regression tests, but the old direct-video trainer still defaults to origin-zero rays.
- PowerFoam raytrace starts must be tied to the ray's front power cell, not "nearest cell anywhere along the ray." On real DeepView oblique views, per-ray closest-line starts skipped front cells and undercovered streaming alpha; near-plane power starts plus regular-triangulation connectivity cut sampled mean alpha error to `0.018`/`0.080`, while Cech/AABB still undercovered at `0.374`/`0.410`. The remaining blocker is regular graph speed/trainer integration, not just rescuing nonzero alpha.
- Regular-triangulation plus near-plane starts is now the best clean DeepView PowerFoam row, but still not paper-accepted: OPENCV_FISHEYE appearance-only 16f/1024-cell heldout improved from Cech/AABB `10.8536/0.0766` to `12.5099/0.1169`, then source-view appearance training overfit heldout to `12.4527/0.1160`. The final gap is not Metal trainability; it needs either stronger geometry/support or a training objective that improves heldout after init.
- The remaining regular-row gap is not fixed by cheap compositing or simply keeping every clean point. A best-fit constant background only lifts the 1024-cell regular heldout to `12.7958/0.1228`, and a 2714-cell all-filtered regular step-0 probe reaches PSNR `12.6368` but drops SSIM to `0.0942`. Background/point count are bounded levers; prioritize geometry support quality or heldout-improving objectives.
- HLOC can bypass the pycolmap-ONNX blocker but is not automatically a stronger clean init on local DeepView probes: after fixing actual `aliked-n16rot` selection, feature caps, and known-pose verification, the wide 2-camera smoke produced `0` points and a close-overlap 4-camera frame-0 smoke produced only `2` verified two-view points. Treat HLOC as an honest backend now, not as evidence that ALIKED/LightGlue will close the paper gap without a denser GPU/full-resolution artifact.
- Official COLMAP CLI plus cuDNN is the working Modal ONNX route for ALIKED/LightGlue, but ONNX startup is not geometry quality. Real DeepView COLMAP-CLI probes stayed sparse: wide2/128px brute force `0` points, near4/512px brute force `9`, near4/512px LightGlue `27` with only two-view tracks and reproj median `6.25px`, and guided verification pruned that LightGlue probe to `0`; do not spend full 1024px ALIKED runs unless a cheap probe first clears a much denser track gate.
- The selected regular DeepView row is not missing an early appearance-training peak. A 12-step dense-eval run with 4x slower `texel_sv_rgb` LR increased source PSNR `12.7537 -> 12.7685` while heldout PSNR monotonically fell `12.5099 -> 12.5057`; source-only appearance updates overfit immediately even under smaller LR.
- The selected regular DeepView row is not mainly a color/background bug, a frozen-support bug, or only the hard `camera_0040` holdout choice. Train-fit background+channel affine can push heldout PSNR to `13.9892` but SSIM only to `0.1416`; support/material thaw improves source `12.7537/0.1301 -> 12.7753/0.1313` while heldout drifts to `12.5037/0.1170`; normal-thaw moves normals/quaternions but also keeps best heldout at step 0; a reused-PLY LOTO diagnostic on `camera_0003` reaches `13.2527/0.1270` but still misses SSIM. The remaining gate is spatial/depth/material/objective quality, not cheap color/compositing, shallow support thaw, or simply picking an easier in-rig heldout.
- Official PowerFoam CUDA/Warp fixture parity depends on the effective upstream raster texture temperature, not just the source fixture's requested option: the pinned official raster texture path behaves as `texel_temperature=10.0`. Keep fixture metadata and local comparison options aligned to that value before chasing SV color math.
- For the cheap dynamic CUDA fork, cite dated strict micro artifacts, not `latest`: `cuda_micro_time_causality_20260506` proves time conditioning/warm timing, while `cuda_micro_blackbg_20260506` is the exact fixed-background CUDA-vs-Metal comparison. `latest` is only a 128px deployment reference.
- CUDA-vs-Metal smoke comparisons must match initialization and background, not just clip/size/steps. The exact fixed-black comparison uses random Metal init and black backgrounds, yielding Metal eval PSNR/SSIM/L1 `5.1222/0.0105/0.5057` versus official CUDA `5.5640/0.0284/0.4901`.
- Height+SV material sampling had a real endpoint bug: when height clipped the far side of the interval, material lookup still used the near endpoint. The fixed Metal kernels now route material and `g_t` through the height-clipped endpoint and pass raytrace plus independent PyTorch texture-reference checks, but the selected DeepView checkpoint re-render stayed at `12.5075` PSNR, so this was correctness debt rather than the heldout-quality blocker.
- Synthetic regular-triangulation parity and real-scene raytrace quality exercise different start semantics. Tiny dense-vs-regular tests still pass with origin/zero-near starts, but real positive-near camera rays need near-plane power starts; don't use the synthetic origin-start success to dismiss real-view start/connectivity diagnostics.
- On the selected DeepView PowerFoam checkpoint, Cech/AABB sphere overlap is not a conservative proxy for regular-triangulation ray walking: each frozen frame has `7207` regular edges but only `3389` shared Cech edges (`47.0%` coverage), with all `3818` missing regular edges per frame being non-overlapping under `r_i + r_j`. First-sphere-hit starts improve Cech sampled alpha but still leave `0.264`/`0.310` mean alpha error on the bad oblique views, so Cech's real-scene undercoverage is graph topology, not just start-cell choice.
- The selected regular DeepView heldout failure is high-alpha/high-support, not blank coverage: heldout alpha mean is `0.9776`, alpha `>0.9` covers `97.1%` of pixels, the worst-frame sphere-support proxy hits `99.99%` of pixels, and high-alpha pixels carry `95.7%` of total residual. The remaining paper gate needs spatial/depth/material/objective work, not another coverage-only support patch.
- On the selected regular DeepView row, finite camera nudges, official-objective losses, compressed warmups, and all-filtered capacity are bounded improvements, not acceptance fixes. Camera perturbations only lift the frozen subset to best PSNR/SSIM `12.6926/0.0900` or `12.6781/0.1205`; MSE + `0.2*SSIM` + random background + normal/contribution/interpenetration reaches `12.5535/0.1255` at 1024 cells after short warmups; all `2714` train-visible points select step 10 at `12.6689/0.1000`. The failure is still structural/depth/material/track quality, not just pose jitter, long warmups, official loss weights, or sampled point count.
- Full8 1024px COLMAP-CLI ALIKED/LightGlue works technically on Modal but is not a dense clean-init fix for DeepView: it produced only `319` points despite `496` verified image pairs, with good long two-camera/multiframe tracks (`track_p90=8`, unique-frame p90 `4`) but far below the paper verifier's `>=2000` point gate. The selected 2714-row residual diagnostic is also high-alpha/high-support (`96.4%` alpha>0.9, worst-frame support hit `99.87%`, high-alpha residual share `95.5%`), and a stronger train normal-distance weight (`1.0 -> 0.5`) reduced mean normal-distance but stayed at `12.6686/0.1000`; the next lever needs spatial/depth/material ordering, not ALIKED deployment or global normal loss.
- Official PowerFoam normal supervision is not equivalent to this repo's scalar Metal `normal_distance` loss. Upstream requests median depth, renders a normal map, and supervises normals against Metric3D or filtered-rendered-depth normals; our Metal aux path can report normal/depth diagnostics, but the differentiable training path only exposes RGB/alpha/scalar normal-distance. Treat differentiable rendered normals plus median-depth/self-normal supervision as the next real "proper PowerFoam" gap, not another weight sweep.
- The Metal raytrace height+SV path now exposes differentiable rendered normals: `return_rendered_normal=True` reaches `powerfoam_raytrace_forward` normal accumulation and `raytrace_height_sv_backward` routes `grad_out_normal` into density/transmittance and normal-frame feature slots. This closes the first prerequisite for official-style normal-map supervision, but does not by itself clear the DeepView paper-quality gate.
- Nearest-camera clean support got PowerFoam over the PSNR gate but not the SSIM gate: the `nearest0040` 3543-point SIFT artifact reached heldout `13.2663/0.1117`, and 9 texel sites after the Metal `feature_dim<=384` backward fix reached `13.2797/0.1122`. Train-fit color affine can lift the 4-site nearest row to `14.1396/0.1454`, but SSIM-optimized affine and higher texel capacity overfit source rather than clearing `0.15`; the remaining blocker is heldout-stable structure/material, not raw point/site capacity.
- Heldout-blind train-fit RGB matrix calibration turns the nearest0040 init into an accepted eval row (`14.3841/0.1556` at step 0), but the same raw render is only `12.6907/0.1246`. Keep this disclosed as calibrated evaluation evidence with its transform artifact and uncalibrated metrics; it is not proof that raw PowerFoam Metal has solved the DeepView structure/material gap.
- The first same-split nearest0040 free-dynamic 3DGS comparator is worse than raw PowerFoam on PSNR but not on SSIM: 3543-splat fast_mac/pinhole 3DGS reaches heldout `10.9809/0.1133/0.2043`, while raw PowerFoam reaches `13.2663/0.1117/0.1691`. This supports "PowerFoam is not simply worse than raw splats on PSNR" but keeps the main SSIM/structure problem alive; strict projection parity is still open because the splat trainer uses pinhole while PowerFoam uses OPENCV_FISHEYE.
- CUDA dynamic-geometry smoke must require alpha/support motion, not just time-varying RGB. The fixed-black `cuda_dynamic_geometry_micro_20260506` run proves the split: feature-only CUDA has `time_alpha_delta_mean=0.0`, while scene-side center/radius/quaternion/height coefficients produce `time_alpha_delta_mean=0.002022` and support delta `0.003174` without touching Warp raster/raytrace kernels.
- Metal dynamic-geometry proof must not use feature drift as the motion proxy. The geometry-only explicit-video RBF run disables dynamic features and still reaches temporal screen delta `0.4457px`, alpha delta `0.00872`, support delta `0.00754`, and feature temporal delta `0.0`; nearest0040 remains open because the dynamic trainer is plain-video only.
- PowerFoam completion audits should reload linked configs and enforce negative controls, not just trust summary labels. The P0 audit now checks nearest0040 split/seed/sampling from config files, Metal geometry-only smoke scope and artifacts, and CUDA feature-lane RGB-only versus geometry-lane alpha/support deltas; this prevents stale TODO rows from looking complete by artifact name alone.
- Dynamic-geometry evidence should be training A/B evidence, not only tests: on the same 16f/64px/1024-cell video, geometry-only RBF with features frozen beat fixed-geometry color-only on mean/min SNR (`14.8959/13.5512` vs `14.6847/12.6700`) and mean/min PSNR (`19.5667/18.2504` vs `19.3554/17.3692`) while color-only had lower L1. Use per-frame SNR/PSNR plus alpha/support motion for this claim, not a single loss.
- For video motion benchmarks, match the trainer's temporal sampling to the ranker: `load_video_sequence` reads consecutive frames, so a 4fps/16f motion-ranked YouTube window must be materialized or loaded through a future `sample_fps` contract. On a 1280x640 sampled YouTube clip at 128px/16f/1024 cells, geometry-only beat fixed-geometry repaint on all tracked quality scalars (`+0.495` mean SNR, `+0.513` min SNR, `-0.0140` L1, `-0.00714` MSE) while moving screen geometry `1.07px` mean / `2.53px` p95 and keeping feature temporal delta `0.0`.
- 512px dynamic PowerFoam is train-step cheap enough at fixed 1024 cells, but artifact logging dominates wall time: on the center-cropped 8fps YouTube clip, 40 one-frame 512px steps took `~7.7s` inside the train loop while full process wall time was `25-28s` because initial/final eval and MP4 writes are large. Geometry-only still beat fixed-geometry repaint by `+1.06` mean SNR and `+2.06` worst-frame SNR, with `4.30px` mean screen motion and zero feature drift.
- Solid-green local MP4 playback can be a media writer/player bug even when tensors are fine: OpenCV `VideoWriter(..., fourcc='mp4v')` produced MPEG-4 Part 2 files that ffmpeg/OpenCV decoded correctly but local player/QuickTime showed as green. Use ffmpeg/libx264 H.264 (`avc1`, `yuv420p`) for saved benchmark videos; the dynamic/powerfoam Metal loss and val paths already share `model(indices) -> render_features_to_rgb(...)`.
- Full-clip all-enabled feature PowerFoam is stable but does not solve visible coarseness by itself: F32/token RBF with all dynamic channels, 8 texel sites, and 56f/512px improves mean PSNR `17.2776 -> 18.0796`, yet remains visibly cellular; a 2048-cell short follow-up starts undercovered (`alpha_mean 0.878`) and reaches only `17.7607` mean PSNR at 60 steps. Treat coarseness as support/init/schedule quality, not merely frozen appearance.
- Learned-camera dynamic PowerFoam cannot train by just feeding learned rays into the current Metal op: the op produced no gradients to ray origins/directions in the MPS probe. The working first path decodes `camera_to_world(t)`, transforms foam points/normals/tangents into camera coordinates in Torch, and renders with fixed origin rays; a 1-step MPS smoke then moved camera rotation `0.29deg` and translation `0.027`.
- The first real 512px/56f learned-camera dynamic PowerFoam run is a negative diagnostic, not a win: it moved the camera (`4.54deg`, `0.304` translation) and improved from `8.05 -> 11.29` mean PSNR, but badly trailed the fixed-camera all-enabled baseline (`18.08` mean PSNR, `0.089` L1). Object-centric random foam plus learned camera creates a harder support/init problem; next try residual camera around the stronger fixed-camera/image-plane gauge or a camera-clamp sweep.
- Residual learned camera around the fixed-pinhole/image-plane gauge preserves high-res quality: with base camera `[0,0,0] -> [0,0,1]`, the 512px/56f run started at the fixed-camera baseline (`17.2777` PSNR), ended at `18.0871` mean PSNR / `0.08928` L1, and kept camera motion tiny (`0.133deg`, `0.0116` translation). Use this as the current nice-video camera branch, not the object-centric random run.
- Orbit-camera/static-dynamic PowerFoam did not rescue the learned-camera support problem: the corrected 180deg orbit init with source-camera normals, 128 dynamic / 896 static cells, `static_only_steps=60`, and `no_repaint_steps=90` peaked at step 60 (`11.0654` mean PSNR, `0.23630` L1) then regressed by step 180 (`10.3100`, `0.26030`), far behind the fixed/residual image-plane branch (`~18.08` PSNR, `~0.089` L1). Treat orbit/world-space backprojection as a negative diagnostic unless the clip is calibrated/synthetic orbit; residual around the fixed-pinhole/image-plane gauge remains the usable high-res branch.
- Integrated-drone PowerFoam camera dynamics are powerful enough to be dangerous: the 512px/56f/180-step run moved much more than residual camera (best step 120: `21.3deg`, `0.790` translation, `12.20` PSNR / `0.2147` L1; final step 180: `82.9deg`, `0.253`, `7.89` PSNR / `0.3471` L1). This proves the head can escape tiny residual motion, but the current caps/LR over-rotate and need a clamped sibling before quality comparison.
- Progressive camera-prefix curriculum plus clamped integrated-drone caps prevents the learned-camera spin but still under-recovers hard camera motion: the 512px/56f run improved to final `13.08` mean PSNR / `0.1638` L1 with `10.72deg` mean rotation-from-base and `36.18deg` summed adjacent rotation, versus the old drone final `7.89` / `0.3471` and `82.9deg` / `864.75deg`; it remains far below the residual fixed-gauge branch (`~18.08` / `0.0893`) and only learns a modest ~30deg first-to-last path, not a full apparent 180deg arc.
- For fast-mac feature forks, keep explicit Metal pixel zero/write even when feature background is zero: an attempted `torch::zeros` allocation plus skipped shader init produced repeated-call parity failures, while a copied `f32_zero_bg` fork that only skips the final background-tail add kept parity and won bounded `B16/G8192/F32` rows. Do not mutate stable `v6_refined_features` for this class of experiment.
- In F32 feature splatting, `transmittance_threshold` is only top-p-like per pixel; backward mostly pays the tile max stop prefix. Raising `alpha_threshold` is the real support-pruning lever, but the safe value is not simply the fastest one: on the 256px goodset F32 `v5_features` trainer, `1/128` cut W&B runtime `26.01m -> 18.37m` and improved heldout `12.7536/0.1716/0.1729 -> 13.6248/0.1922/0.1561` PSNR/SSIM/L1, while `1/64` was faster (`14.27m`) but source PSNR collapsed to `18.20` mean and heldout PSNR stayed near default. Promote from heldout media/metrics, not raster speed alone.
- The promoted F32 goodset relpose run is not physical-camera recovery yet: the `1/128` checkpoint predicts only `0.622deg/0.0311` for `camera_0006 -> heldout camera_0005` versus calibrated `18.631deg/0.1851`, and `1.589deg/0.1292` for `camera_0006 -> camera_0014` versus `33.186deg/0.3094`. The full relpose head starts all queries at identity by zero-init, the frozen rig offsets stay zero, and the active trainer discards DeepView fisheye metadata into pinhole `CameraSpec`s; future heldout rows need pose-error diagnostics plus a fisheye-preserving A/B before treating PSNR as learned camera-offset evidence.
- Token-layout ablations break several hidden `tokens == decoded splat tokens` assumptions. When `model.tokens` includes non-decoded world/register/detail tokens, renderer auto-selection, gaussian-count prints, browser export slices, and static/dynamic split validation must count active decoded token spans, not total query tokens.
- Current multicam relpose feature training is only fast when the 512px branch is absent/rare: local phase timing on the fast512 token-budget config gives 64/128/256px steps at `0.66/1.07/2.22s`, but 512px jumps to `10.25s` with `6.81s` in backward; the weighted schedule is about `1.7s/step` before validation/media, so use trainer-side `profile_timing` and treat W&B wall time as logging/media-contaminated until proven otherwise.
- For F32 fast-mac feature splatting, v7/v8/v9 hardware/front-K lineages are not clean training ports; the useful v8 idea was host metadata, and the current best opt-in shader is `v11_features_gradcache_zero_bg_hostmeta_fixedbin`, which only gives an incremental direct/trainer win and remains no-overflow/fixedbin-memory bounded.
- F32 colorize/loss fusion has two separate truths: no-prenorm L1 image-space gradient production is worth a native kernel (`v12a` gives `5.9x` at N16/256 and `9.25x` at N32/512 versus trainer-style Conv2d+compose+L1 backward), but naively moving colorize into the raster tile loop is slower (`v12c` MSE full-fused is `0.66x` at B16/G8192/256 and `0.60x` at 512). The next fusion boundary should keep image-space color math vectorized or feed compact RGB grads into raster backward, not scalarize the whole colorizer inside tile traversal.
- v12a's no-norm fused producer is resolution-dependent, not a universal trainer win: on 128px fixed-render it loses to simple Torch no-norm L1 (`188.9ms` vs `174.7ms` median) while still beating LayerNorm/pre-norm (`265.6ms`), so promote it only after 256/512 end-to-end A/Bs; for tiny fast iteration, dropping pre-norm matters more than fusing the producer.
- DSSIM is worth a native image-space VJP on MPS but should not start inside the splat tile loop: a standalone Metal reflect-window DSSIM+`grad_rgb` probe matches PyTorch gradients and beats PyTorch autograd by `2.6x/3.5x/3.3x` at N16 128/256/512, giving a clean `prediction,target -> grad_rgb` boundary for `standard_gs`.
- The Metal DSSIM/v13 boundary should stay image-space until the final raster handoff: `losses.dssim_backend="metal"` improves the 128px F32 relpose fixed-render median `269.9ms -> 229.0ms` and full-step median `482.3ms -> 434.6ms`; `v13a` exact recompute is a memory valve but slower in microbench (`20.3ms -> 31.1ms` at B2/G8192/F32/128) and only a modest current-allocator valve in full train (`1524.6 -> 1417.7MiB`, driver peak unchanged), while batch scheduling tricks are bounded (`serial 31.0ms`, active-auto 20.2ms, active-on 22.0ms, recompute 23.6ms). `v13b` RGB-grad handoff is still only the live speed candidate, not a proven runtime drop: the implemented scaffold would replace dense F32 backward input with RGB grads (`528MiB -> 48MiB` at B16/512/F32), but the current runnable trainer path remains v11-compatible until the Metal handoff kernel exists. Tile-only temporal masks look weak in dense synthetic rows because active tiles saturate near 1.0; pair-level masks need real projected-state profiles.
