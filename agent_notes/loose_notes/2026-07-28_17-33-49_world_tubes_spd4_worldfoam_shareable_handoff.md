# Shareable World Tubes / SPD(4) / WorldFoam handoff

- Time: 2026-07-28 17:33:49 +0900
- Role: synthesis and evidence-audit pass
- Objective: consolidate the full representation, math, shader, training,
  failure, paper, safety, and next-work history into one document suitable for
  a new collaborator.

Created:

```text
research_notes/world_tubes_spd4_worldfoam_handoff_2026-07-28.md
```

The handoff distinguishes observed facts, mathematical identities,
computational evidence, refuted interpretations, unrun code, and open
publication gates. It incorporates the July 28 correction that the selected-
time 21-row matrix is representation/cost context rather than causal compiled-
atlas evidence, and records the still-unrun frozen identical-world executor as
implementation rather than a result.

Inputs included the current project/status indexes, representation audit,
SPD(4) physical-renderer handoff, WorldFoam material-selection note, paper
closure audit, ordered-transfer registration, current artifacts, source tree,
and dirty-worktree status. No device work was launched.

Final cross-check additions:

- recorded the tested one-chart moving-camera SPD(4) compiler and its
  `22 passed` CPU gate, while preserving the nonlinear/projective limits;
- recorded that the direct benchmark and single-run paper runner expose the
  new physical axes, but the higher-level matrix runner does not yet propagate
  them;
- recorded retained-path implementation limits, including all-atom fixed
  quadrature, undifferentiated topology, and the current RGB-only production
  sequence contract;
- corrected the umbrella verification snapshot to `142 passed, 4 skipped`,
  distinguished sampled MPS process peaks from representation payload, and
  clarified that three accepted protocols contain nine method/seed lane rows;
- verified all 59 relative Markdown links resolve;
- independently verified the bounded SPD(4) aggregate with the
  standard-library report checker;
- passed scoped `git diff --check`.
