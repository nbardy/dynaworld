# Raw Metal Fast Rasterizer Import

Created `shader_experiments/raw_metal_fast_rasterizer/` and copied the raw
downloaded rasterizer artifacts into it:

- `README.md`
- `CUDA_NEXT.md`
- `mlx_projected_gaussian_rasterizer.py`
- `metal_gsplat.tar.gz`

The prompt listed `README.md` twice, but both references pointed at the same
source path, so the destination contains one copy. I inspected the tarball before
copying; it contains the same three source/doc files plus a `__pycache__`, so I
left it packed rather than unpacking generated bytecode into the repo.

No `key_learnings.md` update: this was a straightforward artifact import, not a
surprising project lesson.
