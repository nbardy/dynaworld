# README progress section

Added a top-level `## Progress` section to `README.md` based on the user's
requested status inventory.

What changed:

- Replaced the short `## Status` paragraph with a structured checklist.
- Recorded completed local baselines: TokenGS reproduction, implicit camera
  baseline, and fast differentiable Gaussian rasterizer on Mac local GPU.
- Recorded next baseline targets: video diffusion features and single-step
  Marigold-style features.
- Captured open model architecture questions around time handling, longer
  videos, sliding-window training, novel camera support, and camera/video
  representation disentanglement.
- Captured future directions around auto-research, "World Model at Home",
  contributor-run experiments, async home-GPU training, and splat decoding as a
  possible video-generation inductive bias.

No `agent_notes/key_learnings.md` update was made because this was status
documentation rather than a new surprising technical lesson.
